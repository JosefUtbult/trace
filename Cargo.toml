[package]
name = "trace"
version = "0.1.0"
edition = "2024"

[features]
no-color = []
std = ["critical-section/std"]

[dependencies]
critical-section = { version = "1.2.0"}
fixed_string = { path = "lib/fixed_string" }
